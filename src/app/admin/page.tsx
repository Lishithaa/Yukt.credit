export const dynamic = "force-dynamic";
import { prisma } from "@/lib/prisma";
import { StatsCard } from "@/components/stats-card";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
export default async function AdminPage(){ const [caCount,msmeCount,lenderCount,appCount,disbursed]=await Promise.all([prisma.cAProfile.count(),prisma.mSMEProfile.count(),prisma.lenderProfile.count(),prisma.loanApplication.count(),prisma.loanApplication.aggregate({where:{status:"DISBURSED"},_sum:{loanAmount:true}})]); return <div className="mx-auto max-w-7xl space-y-6 px-4 py-8"><h1 className="text-2xl font-bold">Admin Portal</h1><div className="grid gap-4 md:grid-cols-4"><StatsCard title="CAs" value={`${caCount}`} /><StatsCard title="MSMEs" value={`${msmeCount}`} /><StatsCard title="Lenders" value={`${lenderCount}`} /><StatsCard title="Applications" value={`${appCount}`} /></div><Card><CardHeader><CardTitle>Total Disbursed</CardTitle></CardHeader><CardContent>₹{(disbursed._sum.loanAmount || 0).toLocaleString("en-IN")}</CardContent></Card><div className="grid gap-6 md:grid-cols-2"><Card><CardHeader><CardTitle>User Management</CardTitle></CardHeader><CardContent>Approve/block CAs and lenders (MVP mock).</CardContent></Card><Card><CardHeader><CardTitle>AI Model Configuration</CardTitle></CardHeader><CardContent>Uses calculateCreditScore; replaceable with external model later.</CardContent></Card></div></div>; }
